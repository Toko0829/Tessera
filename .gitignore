# ── Node / Angular (apps/web) ──
node_modules/
dist/
.angular/
*.tsbuildinfo
coverage/
playwright-report/
test-results/
.vitest/

# ── .NET (apps/api, apps/worker) ──
bin/
obj/
[Bb]uild/
*.user
*.suo
.vs/
TestResults/
*.trx
*.coverage
*.received.*

# ── Secrets / local config (never commit — CLAUDE.md §6) ──
# Env files (all variants), except the checked-in template
.env
.env.*
*.env
!.env.example
# Keys, certificates, credentials
*.pem
*.key
*.pfx
*.p12
*.crt
*.cer
*.keystore
credentials
.aws/
# .NET secrets / machine-local overrides
appsettings.*.Local.json
appsettings.Local.json
secrets.json
**/secrets/

# ── Tooling / OS / IDE ──
.claude/
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.log
.DS_Store
Thumbs.db
