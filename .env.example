# Copy to .env for local docker-compose overrides. .env is gitignored — never
# commit real credentials. These values are for the local dev database only.
POSTGRES_PASSWORD=tessera_dev
MINIO_PASSWORD=tessera_dev_minio
